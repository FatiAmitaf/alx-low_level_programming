#include <stdio.h>
/**
 * main - Prints a random number and states whether it is pos neg or zero
 *
 * Return: 0 (success)
*/
int main(void)
{
char l = 'a';
while (l <= 'z')
{
putchar(l);
l++;
}
putchar('\n');
return (0);
}
