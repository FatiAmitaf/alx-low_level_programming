#include "main.h"
/**
 *_islower -  function prints the alphabet in lowercase
 *
 * Return: 1 if it is lowercase 0 otherwise
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1); 
}
else
{
return (0); 
}
}
